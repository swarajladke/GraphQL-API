# Bun sets NODE_ENV=test for `bun test`, so it auto-loads `.env.test`.
# Copy this file to `.env.test` and keep it pointed at a THROWAWAY database:
# the test suite truncates all tables between tests.
DATABASE_URL="postgresql://booking:booking@localhost:5433/booking_test?schema=public"
